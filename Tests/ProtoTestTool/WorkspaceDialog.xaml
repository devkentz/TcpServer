<Window x:Class="ProtoTestTool.WorkspaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Workspace 설정"
        Height="200" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Workspace 폴더를 선택하세요"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource HeaderTextBrush}"
                   Margin="0,0,0,15"/>

        <!-- Path Selection -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PathTextBox"
                     Grid.Column="0"
                     VerticalContentAlignment="Center"
                     IsReadOnly="True"
                     Margin="0,0,10,0"/>

            <Button x:Name="BrowseBtn"
                    Grid.Column="1"
                    Content="찾아보기..."
                    Width="100"
                    Click="BrowseBtn_Click"/>
        </Grid>

        <!-- Description -->
        <TextBlock Grid.Row="2"
                   Text="스크립트 파일(.csx)과 Proto 파일이 저장될 폴더입니다."
                   Foreground="{StaticResource SmallTextBrush}"
                   FontSize="11"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="OkBtn"
                    Content="확인"
                    Width="80"
                    IsEnabled="False"
                    Click="OkBtn_Click"/>
            <Button x:Name="CancelBtn"
                    Content="취소"
                    Width="80"
                    Margin="10,0,0,0"
                    Background="{StaticResource PanelBrush}"
                    Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
